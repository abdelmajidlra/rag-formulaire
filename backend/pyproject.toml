[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ircc-backend"
version = "0.1.0"
description = "FastAPI wrapper around rag_formulaire"
requires-python = ">=3.10"
dependencies = []
